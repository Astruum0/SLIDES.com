{% extends 'base.html.twig' %}

{# {% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('app') }}
{% endblock %} #}

{% block body %}
    <div id="main">
        <div onclick="location.href='{{path("security_registration")}}' ; " class="button play"></div>
        <div class="button editor"></div>
        <div class="button selector"></div>
        <div class="button rules"></div>
    </div>
{% endblock %}
